Validar se usar com Python ou Node.js será interessante pela manipulação das variaveis de ambiente

se node então instalar na raiz do projeto, 

```bash
npm install selenium-webdriver chromedriver
```