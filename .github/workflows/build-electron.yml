name: Build Electron Package

on:
  workflow_dispatch:
    inputs:
      build-windows:
        description: 'Do you want to build for Windows?'
        required: false
        default: 'yes'
  push:
    branches:
      - main

jobs:
  build-windows:
    # if: ${{ github.event.inputs.build-windows == 'yes' }}
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18.10.0'

      - name: Install root dependencies
        run: npm install 
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Install dependencies for client (frontend)
        run: |
          cd client
          npm install --legacy-peer-deps
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Install dependencies for server (backend)
        run: |
          cd server
          npm install
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Build App for Windows
        run: npm run build
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Zip Windows package (from dist folder)
        run: |
          $VERSION = (node -p "require('./package.json').version")
          Compress-Archive -Path dist/* -DestinationPath "windows-$VERSION.zip"
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Check if Windows zip exists and is not empty
        run: |
          if (!(Test-Path "windows-$VERSION.zip") -or ((Get-Item "windows-$VERSION.zip").length -eq 0)) {
            Write-Error "Windows zip was not created or is empty!"
          }
        shell: pwsh

      - name: Create or Update GitHub Release for Windows
        id: create_release_windows
        uses: actions/create-release@v1
        with:
          tag_name: v${{ github.sha }}
          release_name: Release ${{ github.sha }}
          body: 'Release notes for Windows version ${{ github.sha }}'
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Upload Windows package to GitHub Releases
        run: |
          VERSION=$(node -p "require('./package.json').version")
          curl -L -H "Authorization: token ${{ secrets.GH_TOKEN }}" \
          --data-binary @"windows-${VERSION}.zip" \
          -H "Content-Type: application/zip" \
          "https://uploads.github.com/repos/${{ github.repository }}/releases/${{ steps.create_release_windows.outputs.id }}/assets?name=windows-${VERSION}.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

  build-ubuntu:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18.10.0'

      - name: Install root dependencies
        run: npm install  
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Install dependencies for client (frontend)
        run: |
          cd client
          npm install --legacy-peer-deps
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Install dependencies for server (backend)
        run: |
          cd server
          npm install
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Install required packages for Linux build
        run: sudo apt-get update && sudo apt-get install -y fakeroot rpm 
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Build App for Ubuntu
        run: npm run build
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Zip Ubuntu package (from dist folder)
        run: |
          VERSION=$(node -p "require('./package.json').version")
          zip -r "ubuntu-$VERSION.zip" dist/
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Check if Ubuntu zip exists and is not empty
        run: |
          if [ ! -s "ubuntu-$VERSION.zip" ]; then
            echo "Ubuntu zip was not created or is empty!"
            exit 1
          fi

      - name: Create or Update GitHub Release for Ubuntu
        id: create_release_ubuntu
        uses: actions/create-release@v1
        with:
          tag_name: v${{ github.sha }}
          release_name: Release ${{ github.sha }}
          body: 'Release notes for Ubuntu version ${{ github.sha }}'
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Upload Ubuntu package to GitHub Releases
        run: |
          VERSION=$(node -p "require('./package.json').version")
          curl -L -H "Authorization: token ${{ secrets.GH_TOKEN }}" \
          --data-binary @"ubuntu-${VERSION}.zip" \
          -H "Content-Type: application/zip" \
          "https://uploads.github.com/repos/${{ github.repository }}/releases/${{ steps.create_release_ubuntu.outputs.id }}/assets?name=ubuntu-${VERSION}.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
